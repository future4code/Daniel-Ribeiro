/*
a) Procure uma forma de converter libra (lb) para quilograma (kg) e escreva um programa que 
converta 20lb para kg. Imprima  a resposta no console da seguinte forma: 
20lb equivalem a X kg
*/

/* 
libra = 20
res = libra / 2.2046

alert(`${libra}lb equivalem a ${res.toFixed(2)}kg.`)
console.log(`${libra}lb equivalem a ${res.toFixed(2)}kg.`) */

/* b) Procure uma forma de converter onça (oz) para quilograma (kg) e escreva um programa 
que converta 10.5oz para kg. Imprima  a resposta no console da seguinte forma: 
10.5oz equivalem a X kg */

/* 
oz = 10.5 
res = oz / 35.274

alert(`${oz}oz equivalem a ${res.toFixed(2)}kg.`)
console.log(`${oz}oz equivalem a ${res.toFixed(2)}kg.`) */

/* c) Procure uma forma de converter milha (mi) para metro (m) e escreva um programa que 
converta 100mi para m. Imprima  a resposta no console da seguinte forma: 
100mi equivalem a X m */

/* 
mi = 100
res = mi / 0.00062137

alert(`${mi}mi equivalem a ${res.toFixed(2)}m.`)
console.log(`${mi}mi equivalem a ${res.toFixed(2)}m.`) */

/* d) Procure uma forma de converter pés (ft) para metro (m) e escreva um programa que 
converta 50ft para m. Imprima  a resposta no console da seguinte forma: 
50ft equivalem a X m */

/* 
pes = 50
res = pes / 3.2808

alert(`${pes}pes equivalem a ${res.toFixed(2)}m.`)
console.log(`${pes}pes equivalem a ${res.toFixed(2)}m.`) */

/* e) Procure uma forma de converter galão (gal) para litro (l) e escreva um programa que 
converta 103.56gal para litro. Imprima  a resposta no console da seguinte forma: 
103.56gal equivalem a X l */

/* 
galao = 103.56
res = galao / 0.21997

alert(`${galao}gal equivalem a ${res.toFixed(2)}l.`)
console.log(`${galao}gal equivalem a ${res.toFixed(2)}l.`) */

/* f) Procure uma forma de converter xícara (xic) para litro (l) e escreva um programa que 
converta 450xic para litro. Imprima  a resposta no console da seguinte forma: 
450 xic equivalem a X l */

/* 
xic = 450 
res = xic * 0.24

alert(`${xic}xic equivalem a ${res.toFixed(2)}l.`)
console.log(`${xic}xic equivalem a ${res.toFixed(2)}l.`) */

/* g) Escolha ao menos um dos itens anteriores e modifique o programa para que ele 
peça ao usuário o valor da unidade original antes de converter.  */

/* 
mi = Number(prompt('Digite o valor que deseja converter: '))
res = mi / 0.00062137

alert(`${mi}mi equivalem a ${res.toFixed(2)}m.`)
console.log(`${mi}mi equivalem a ${res.toFixed(2)}m.`) */